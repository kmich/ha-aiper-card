name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - name: Fail if dist/ is stale
        run: |
          if ! git diff --quiet -- dist/; then
            echo "::error::dist/aiper-card.js is out of date. Run 'npm run build' and commit the result."
            git diff --stat -- dist/
            exit 1
          fi
